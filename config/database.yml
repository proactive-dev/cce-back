development: &defaults
  adapter: mysql2
  encoding: utf8
  database: cryptbank_development
  username: root
  password: admin
  pool: 10
  reconnect: true

test:
  <<: *defaults
  database: cryptbank_test

production:
  <<: *defaults
  database: cryptbank_production
